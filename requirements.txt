# Core ML stack
torch>=2.0.0
numpy>=1.24.0

# CUDA kernel extension builds (torch.utils.cpp_extension / load_inline)
# REQUIRED — without ninja every load_inline kernel fails to build.
ninja>=1.11.1
setuptools>=68.0.0  # needed by load_inline on newer torch / isolated envs

# Fused ternary matmul kernel (GPU only)
triton>=3.0.0

# Gigatoken
gigatoken
